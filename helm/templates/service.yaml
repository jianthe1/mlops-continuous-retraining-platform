apiVersion: v1
kind: Service
metadata:
  name: fastapi-mlops-service
  namespace: {{ .Values.namespace | default "mlops" }}
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: fastapi-mlops